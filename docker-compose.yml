version: '3.8'

services:
  postgres:
    image: postgres:13
    environment:
      POSTGRES_DB: codesync
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://user:password@postgres/codesync
    depends_on:
      - postgres

volumes:
  postgres_data: